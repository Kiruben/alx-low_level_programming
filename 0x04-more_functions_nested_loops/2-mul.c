/**
 *mul - prototype function
 *@a: first number to multiply by
 *@b: second number to multiply by
 *Description: function to multiply two integers
 *Return: return multiple of two intergers
 */
int mul(int a, int b)
{
return (a * b);
}
