/**
 *_abs - prototype function
 *@num: number to evaluate
 *Decription: Prints absolute value of number
 *Return: returns absolute value of number
 */
int _abs(int num)
{
if (num < 0)
return (-1 * num);
return (num);
}
