#include <stdio.h>
/**
 * main -Entry point
 *Decription: Prints all combinations of
 *songle digit numbers
 *Return: Always 0 (Success)
 */
int main(void)
{
int num;
num = '0';
while (num <= '9')
{
putchar(num);
if (num != '9')
{
putchar(',');
putchar(' ');
}
num++;
}
return (0);
}
